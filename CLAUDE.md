# CLAUDE.md — Kronos A 股落地研究

本仓库是 Kronos（K 线基础模型）的 fork，用于 A 股量化选股研究。
**新接手请先读 [docs/项目总览与分支索引_20260812.md](docs/项目总览与分支索引_20260812.md)**——
六轮实验的分支地图、结论、可复用资产与继续方式都在那一份里。

用户全局约定（语言、Python 版本、日志、Git 提交格式等）见 `~/.claude/CLAUDE.md`，本文件只写
**本仓库特有**的规矩。

## 提交前必查：明文主机地址

**每次提交前扫一遍暂存区**，防止内网主机地址进库：

```bash
git diff --cached | rg -n "[0-9]{1,3}(\.[0-9]{1,3}){3}:[0-9]{4,5}" && echo "⚠️ 疑似明文主机地址，请掩码后再提交"
```

- 掩码格式统一为 `100.**.**.25:8848`（保留首末段，中间两段打码）；
- 连接串一律写成 `dolphindb://<user>:<password>@<host>:<port>` 占位形式；
- 真实连接串只放 `.env`（已 gitignore），提交 `.env.example` 占位版。

**为什么要有这条**：2026-08-12 发现 R6 新增文档漏网了两处明文地址——前几轮做过一轮掩码化，
就默认"已经处理干净"，结果新写的文档没再扫。历史里仍留有 16 个提交含旧地址（CGNAT 段、
公网不可路由，评估为低风险，决定不改写历史）。这条规矩防的是下一次。

## 实验纪律（六轮下来用血换的）

做任何新的信号/模型实验，以下五条不可省——每一条都对应一次真实事故或误判：

1. **预注册**：判读标准写进计划文档、在跑数**之前**冻结，跑完不许改。R4 的 +8.99% 就是靠 R5
   的预注册判据被判死的；
2. **样本外封盘**：留一段谁都没碰过的数据，只跑一次。回测好看与真的有用之间只有这一道闸门；
3. **占位零点门禁**：任何组合引擎先证明**随机信号在它手里赚不到钱**（|AER| < 3%），
   否则测出的 alpha 可能是引擎自己造的（R6 实测：随机信号在微盘档拿到 +56%）；
4. **免费基线对照**：新信号必须跑赢 10 日动量/反转这类零成本因子，否则没有存在理由；
5. **注入式测试**：写完防泄露/因果性测试后，**故意注入缺陷确认它会 FAIL**——本项目出过
   "6/6 通过但关键测试是恒真断言"的事故。

## A 股评估口径（硬性）

- **组合层一律 long-only**（top-k/drop-n），不用多空——A 股融券受限，多空口径不可实盘；
- **分组分析**用 `analyzer.analyze_factor`（`skip_paused=True`、`skip_one_word_limit=True`、
  `delay=1` 的 T+1 语义）；**组合回测**用 `qlib_bt.QlibBacktestEngine`（涨跌停拦截 +
  `only_tradable=True`）。两者在 `/home/user/workspace/AlphaFarmer`；
- **不做这层处理的后果是量化过的**：R6 附录 A 实测，粗口径（不剔停牌/一字板）把分位 spread
  夸大 **7–10 倍**并颠倒因子排名；
- **双基准**：市值加权指数（对外口径）+ 同池等权（剥 beta）。只看前者会把 beta 当 alpha。

## 数据层

`kronos_qlib`（本分支）直连 DolphinDB：point-in-time 成分、后复权 OHLCVA、停牌剔除、构窗。
**不要改它**——9/9 验收测试是后续全部实验的地基；确需改动先补测试。

已知坑（都踩过）：

- `D.calendar` 延伸到 2040 而真实数据止于 2026-08-07，取未来时间戳必须以**数据末日**为界；
- `QlibDataLoader(swap_level=True)` 下 level 0 是 datetime，reshape 一律按 level **名**
  （`unstack("instrument")`），按位置会静默转置；
- instruments 传 **list** 时 `filter_pipe` 被静默丢弃（只 warning）。

## 分支约定

- `integration` = 主线（数据层 + webui，无实验代码）；`master` = 上游镜像，不动；
- `feature/*` 实验分支**已封盘、不合并**——它们是历史记录不是待合入代码，各轮口径互不相同，
  合进一条线只会分不清哪份数字属于哪个口径；
- 实验文档只在各自分支上，读法：`git show feature/xxx:docs/yyy.md`。
