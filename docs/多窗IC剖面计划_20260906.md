# 多窗 IC 剖面（信息是几窗有几）· 描述性分析计划

> 日期：2026-09-06　分支：`feature/h1-readout`（只新增文件）　性质：**零 GPU、纯描述性、不设判据、不重开任何议题**
> 动机：H1/G1/R1 全部呈"2026H1 窗有 IC、2025H2 窗无"，M 两窗反号。现有两窗只能给出"1/2"，
> 证据单位不够。盘上已有跨更多窗的信号 parquet，用同一脚本把窗数做上去。
> 执行解释器：`/home/user/miniconda3/envs/quant/bin/python`。

## 1. 窗与臂（冻结）

评估窗按**半年切、互不重叠**：W1 2024H2（2024-07-01~2024-12-31）、W2 2025H1、W3 2025H2、W4 2026H1（=backtest，至 07-24）。
每窗末尾 +10 交易日缓冲，无尾部截断。

| 臂 | 可用窗 | 来源 parquet（已在盘，只读） | 合法性 |
|---|---|---|---|
| F0_mean（官方权重 zero-shot，canonical） | W1~W4 | paper 窗 baseline mean（W1/W2 切分）、2025H2 F0_mean、backtest F0_mean | 无训练，四窗全合法 |
| M 动量 | W1~W4 | 各窗 M parquet | 免费裁判 |
| G1 三种子 mean | W3~W4 | 既有 | W1/W2 与其 train/val 重叠，**禁用** |
| F1_mean | W3~W4 | 既有 | W1/W2 与其 train/val 重叠，禁用 |
| H1a-kda（seed 42） | W3~W4 | 既有 | 同上；仅并列呈现 |

若 paper 窗 baseline 只有引擎产物而无逐日信号宽表，则 F0 的 W1/W2 用 canonical 协议 **重算推理**（官方权重 + seed 42，csi300，~2h GPU，为本计划唯一例外，须先贴时长估计再跑）。

## 2. 量（与 IC 剖面脚本同口径）

`paper_replication/ic_horizon_profile.py --cumulative`（对齐修正版 49e6875）：csi300 池、k∈{1,5,10} 累计 rank-IC、NW(lag=k−1) t。
输出一张"臂 × 窗"表（k=10 主表，k=1/5 附表），每格 `IC (t)`；末列 = 四窗合并 n≈500 的 IC 与 NW t。

## 3. 事先写死的读法（描述性，不是判据）

- 统计每臂"k=10 IC > 0 且 t > 2"的窗数 / 可用窗数，如实写成 `x/4` 或 `x/2`；
- F0 与 M 四窗符号序列并列，看 2025H2 的"归零"是 Kronos 独有还是价格类信号共有；
- 任何结论措辞上限："描述性观察"，不得出现通过/失败/复活字样；不派生任何判据、不触发任何后续实验。

## 4. 步骤

- [ ] 4.1 确认 W1/W2 的 F0/M 逐日信号 parquet 是否在盘（贴 `ls` 输出）；缺则按 §1 例外重算；
- [ ] 4.2 跑剖面，落盘 `paper_replication/data/ic_multiwindow.json`；
- [ ] 4.3 写 `docs/多窗IC剖面结果_<日期>.md`（§3 读法 + 全表 + 复现命令）；披露式通道提交推送。

纪律：forward 零接触；权重与信号 parquet 只读；提交规范同 CLAUDE.md，`Co-Authored-By: Hugo <shen.lan123@gmail.com>`。
