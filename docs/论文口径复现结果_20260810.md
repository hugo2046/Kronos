# Kronos 论文口径复现结果（P0 · base 规格）

> 日期：2026-08-10
> 分支：`feature/paper-replication`（从 `feature/kda-repr-head` 切出）
> 配套计划：[论文口径复现计划_20260810.md](./论文口径复现计划_20260810.md)
> 上游负结果：[截面路径zero-shot测试结果_20260810.md](./截面路径zero-shot测试结果_20260810.md)
> 解释器：`/home/user/miniconda3/envs/quant/bin/python`（torch / RTX 5090 32GB）
> 数据后端：DolphinDB `dolphindb://admin:***@100.114.247.25:8848`（`.env` 绑定，`.gitignore`）
> 核心问题：**论文 A 股投资模拟（AER 20.8% / IR 1.65，large 规格）在我们数据上、以开源的 base 规格，能否复现出同方向的正超额？**

> ⚠️ **硬约束**：`Kronos-large`（499.2M）未开源（README Model Zoo），可用上限为
> `Kronos-base`（102.3M）。**判定目标对齐论文 base 规格锚点，不是 large 的 20.8%。**

## 0. 阶段 0：论文 base 规格锚点（Table 10 提取）

从 arXiv:2508.02739v1 PDF（第 18 页）附录 Table 10（"Full results of investment
simulation"）逐字提取 CSI300 列（本复现只做 csi300）：

| 模型 | CSI300 AER | CSI300 IR | （对照：CSI800 AER） |
|---|---|---|---|
| Kronos-small | 0.1805 | 1.2394 | 0.1772 |
| **Kronos-base** | **0.1911** | **1.3782** | 0.1867 |
| Kronos-large | 0.2193 | 1.4177 | 0.1974 |

**本复现的判定锚点 = Kronos-base CSI300：AER 0.1911 / IR 1.3782。**

> 勘误：上游文档 §4.1.1 引用的"AER 20.8% / IR 1.65"实际是 Table 10 的 **Average**
> 列（CSI300 + CSI800 平均），不是 CSI300 列；CSI300-only 的 large 数字是 0.2193 / 1.4177。
> 本文档一律用 CSI300 列与 base 规格，避免跨规格 / 跨池混。
> 论文附录确认的口径细节：CSI300 k=50 / n=5、单边成本 0.15%，与本计划 §0 / §2.1 一致。

## 1. 口径对齐表（实际执行）

| 维度 | 论文 | 本复现 | 状态 |
|---|---|---|---|
| 模型 | Kronos_large（最佳） | **Kronos-base**（开源上限） | ⚠️ 不可对齐，判定锚点改 base 规格 |
| 采样次数 N | 20 | **20** | ✅ |
| 回测区间 | 2024-07 ~ 2025-06 | **2024-07-01 ~ 2025-06-30**（242 交易日） | ✅ |
| 调仓 | 每交易日 top-k/drop-n | **每日，k=50，n=5，最少持有 5 交易日** | ✅ |
| 组合 | long-only 等权 | **long-only top-50 等权** | ✅ |
| 成本 | 单边 0.15% | **单边 0.15%** | ✅ |
| 信号 | `mean(pred_close[t+1..t+H])/p_t − 1`，L=90/H=10 | 同左（沿用 cross_section/signal.py 口径） | ✅ |
| 股票池 | csi300 + csi800 | **csi300**（DDB 无 csi800 市场） | ⚠️ 只做 csi300 |
| 基准 | 沪深300 指数 | **`000300.SH` 命中**（市值加权，论文口径）+ 同池等权（门禁用） | ✅ |

九个维度中八个对齐（仅模型规模受开源限制不可对齐）。

## 2. 阶段 1：组合引擎 + 基准（新增模块）

### 2.1 引擎（`paper_replication/engine.py`）

top-k/drop-n long-only 组合引擎（与 cross_section 分组多空的本质区别）。规则严格
按论文/东证描述：每日收盘按信号排序，卖出持有≥5日 且信号最差者，买入未持有 且 信号
最好者，单日换手 = min(n=5, 可卖, 可买)。等权口径：新腿按 1/k 配仓、存量随行就市。

**单测全绿**（`tests/test_paper_replication.py`，§2.2 五条）：

| 测试 | 内容 | 结果 |
|---|---|---|
| §2.2.1 最少持有期 | 持有 <5 日的股票信号变最差也不得被卖 | ✅ 前 5 日换手 0，第 6 日才换 |
| §2.2.2 drop-n | 单日卖出/买入数 ≤ n | ✅ |
| §2.2.3 等权再平衡 | 新买入腿按 1/k 配仓 | ✅ |
| §2.2.4 成本扣除 | 给定换手额净值扣减精确可算 | ✅ |
| §2.2.5 占位门禁 | seed 固定随机信号 AER 不显著为正 | ✅ |

### 2.2 双基准设计（关键偏差，2026-08-10 实测定稿）

计划 §2.3 预设"无指数则用等权池"。实测 `000300.SH` 命中，**指数基准可用**。但实测发现
一个计划未预见的结构性问题，**必须用双基准**：

- **csi300 指数基准**（`000300.SH` 市值加权，论文口径）：用于 AER 锚点比较（规则 2）。
- **同池等权基准**（每日可交易股票等权日收益）：用于**引擎门禁**（规则 1）。

**根因**：等权 top-50 组合天然带 **+4.40% 的等权-beta 溢价**（2024-07~2025-06：全 336 池
等权累计 +17.56% vs csi300 指数 +13.16%）。用指数基准判门禁时，随机占位信号也"复现"
+4.29% AER——这 100% 来自等权 vs 市值加权的结构性 beta 差，不是 alpha，也不是 bug。
20 组独立随机信号 top-50 等权 vs 全池等权日均超额 mean≈0、std=±4.4%，证明引擎不制造 alpha。

**门禁改用同池等权基准后，随机占位 AER≈0（实测 -0.14%），才是"引擎不制造选股 alpha"
的干净判据。** 判读 K/M/R 组的锚点比较与定性判定仍用指数基准（与论文同口径）。
`beta_gap = +4.40%` 作为结构性项并列报告。

### 2.3 数据语义实测：tradestatuscode

本 DDB 的 `tradestatuscode` 真实取值域（全池全期实测计数）：

| 值 | 含义 | 计数 | 可交易？ |
|---|---|---|---|
| -1 | 正常交易 | 72041 | ✅ |
| 0 | 停牌状态 A | 155 | ❌ |
| 2 | 停牌状态 B | 393 | ❌ |
| 4 | 罕见特殊状态 | 11 | ❌ |

本模块 `build_px_tradeable` 按真实语义用 `tradeable = (tsc == -1)`。（注：`cross_section/
windows.py` 用 `==0` 判停牌，只捕获 0、漏 2/4，是既有代码口径，本模块更严格。）

## 3. 阶段 2：N 敏感性试点（先做）

论文声称 IC/RankIC 随采样次数 N 单调改善。取论文窗口前 1 个月（21 交易日）分别跑
N=5 与 N=20，H 日前向收益（`close[t+H]/close[t]-1`，与信号同口径）做 RankIC 标签：

| N | RankIC 均值 | ICIR | 信号截面 std | n |
|---|---|---|---|---|
| 5 | +0.0097 | +0.236 | 0.0506 | 21 |
| 20 | +0.0054 | +0.157 | 0.0500 | 21 |

**N 间差 ΔRankIC = -0.0043，N=5 均值标准误 ≈ 0.0090**（n=21，IC 序列 std≈0.04 →
SE = 0.04/√21）。差值在 1 倍标准误内，**与论文单调改善无显著矛盾**，试点样本太小
不作定量结论。按预注册粗筛（差值 < -2SE 才停）通过，继续全量。

> 首轮误用 1 日次日收益（`shift(-1)`）做标签，与 H=10 信号口径不匹配，IC 失真
> （N=5 +0.0060 / N=20 +0.0024）；改用 H 日前向收益后修复。

## 4. 阶段 3：四组同引擎回测（K/M/R/P）

**待 K 组全量信号（N=20，242 日，断点续跑）完成后填入。** 四组同引擎、同区间、同池：

| 组 | 信号 | 作用 |
|---|---|---|
| K | Kronos-base, N=20 | 被试 |
| M | 10 日动量 | 免费基线 |
| R | 10 日反转 | 免费基线 |
| P | 随机占位（seed=42） | 引擎零点 |

### 4.1 结果总表

_（待 K 组完成后填）_

| 组 | AER(指数) | IR(指数) | AER(等权) | IR(等权) | 最大回撤 | 日均换手 |
|---|---|---|---|---|---|---|
| K | TBD | TBD | TBD | TBD | TBD | TBD |
| M | TBD | TBD | TBD | TBD | TBD | TBD |
| R | TBD | TBD | TBD | TBD | TBD | TBD |
| P | TBD | TBD | TBD | TBD | TBD | TBD |

### 4.2 预注册判读（§4）

_（待四组完成后填）_

## 5. 局限性

- **模型规模不可对齐**：base（102.3M）vs 论文最佳 large（499.2M），论文证实存在 scaling
  law，规模越大单调越好。这是开源限制导致的硬偏差。
- **股票池只做 csi300**：DDB 无 csi800 市场，未做第二组对照。
- **占位门禁口径偏离预注册原文**：原文"占位 |AER|<3%（对称，相对指数）"在"等权组合 +
  市值加权基准"组合下不成立（结构性 beta 击穿阈值）。改用同池等权基准判门禁，是忠实于
  门禁意图（"引擎不制造 alpha"）的必要调整，在跑 K 组前冻结并记录。
- **N 敏感性样本小**：n=21 试点，IC 差异在噪声内，未独立验证论文"N 单调改善"主张。
- **基准绑定 DDB 实例 `100.114.247.25:8848` 与数据末日 2026-08-07**。

## 6. 交付清单

1. **代码**（`paper_replication/`）：config.yaml / common.py / engine.py / benchmark.py /
   signal.py / pipeline.py / run_signals.py / run_backtest.py。
2. **单测**（`tests/test_paper_replication.py`）：§2.2 五条全绿。
3. **结果文档**：本文件。
4. **数据产物（不入库）**：`daily_signals_K/M/R/P.parquet`、`n_sensitivity.json`、
   `backtest_results.json`、`kronos_K.log`。受 `*.parquet` 全局 `gitignore` 覆盖。
