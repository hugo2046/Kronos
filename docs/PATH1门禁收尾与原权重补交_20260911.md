# PATH1 门禁收尾与原权重补交（20260911）

本轮基于 `e4de6464ffa75b522e45294c3bed30698b22fd99`，只修复实际打分、冻结检查与不覆盖行为，纠正结果表述；不训练真实模型，不生成新路径，不读取数据库或forward，不重算并覆盖旧分数/判据。

## 1. 已完成的代码修复

- 生产 `_score_segment` 必须取得当前上下文并传入 `load_best`：绑定协议内容、run、实际G1权重、preflight、标准化统计和缓存清单；检查路径精确键集合和逐chunk字节SHA，不提前读取dev_eval标签。
- 新训练checkpoint与best都保存上下文；加载同时比较内嵌上下文和标记。校验的权重文件必须就是实际加载的epoch文件，禁止“验A读B”。旧best缺完整上下文时不自动补写或升级。
- 冻结清单必须包含且仅包含MEAN/PATH × train/val/dev_eval六份文件，文件名、内嵌臂段身份、SHA、来源上下文、两份头身份、样本键和有限分数均检查；空、缺、多、错名、改字节在标签加载前拒绝。
- 训练、缓存和评价入口拒绝覆盖已有完成产物；不匹配缓存停止而非自动重建。原子写入器也拒绝替换已存在文件。
- 合成回归包含原先会失败的生产反例及完整清单可成功的正例。保留旧样本出口兼容、e0选点和缺一/改一分数的测试。

这些修复不能追认 `e4de646` 当时已经执行了完整身份门禁；本轮也没有重新生成该次结果来掩盖缺口。

## 2. 研究结论勘误

从原路径/标签/分数复算：val的PATH−MEAN日均RankIC差为−0.000283883，dev_eval为−0.000415784，原判据未触发。

dev_eval的PATH MSE `1.4162206733071394` 略优于MEAN `1.4168983390573198`（约0.048%），val的PATH MSE略差；不能说两段MSE都无增量，也没有统计依据断言该差异就是噪声。当前配置没有读出正向排序增量，不等于证明所有路径模型无效。MEAN在已观察样本上保序不代表在任意输入上数学上严格单调。

## 3. 仍待Debian补交的原文件

提交里只有JSON标记，没有以下两份 `.pt`；它们应位于原 worktree 的 `artifacts/experiments/path_information_20260911/heads/`：

| 文件 | 原best标记冻结的SHA256 |
|---|---|
| `head_MEAN_s100_e004.pt` | `69121c105cf7a40e5fc5fc89f5b0e8254bfbd49dfc4fb0744a876a9f4dfbc8ab` |
| `head_PATH_s100_e004.pt` | `d9b3292ab5ed3830b4e7c8543c00a72f2cbdda6df941880f704d9a9fae7a98c1` |

**必须补原字节文件，不重新训练、不重新保存checkpoint来“补相同权重”。** 若遗失，查已有备份并仅接受精确SHA匹配；找不到则明确报告遗失，保持负结果与无法完整重放的限制，不制造替代证据。

## 4. zcode执行步骤（只需这一轮补交验收）

1. 在已有 `/home/user/workspace/Kronos-path-information` worktree `git pull --ff-only origin codex/path-information-plan`，核对用户交接提交。不切换主工作树，不改cron。
2. 运行下方七文件自足测试。本次Mac预期114项；另加 `tests/test_kronos_regression.py` 时，Debian有原HF缓存可跑4项真实回归，合计118。缺缓存则如实报告，不能下载替代模型或把跳过当通过。
3. 对两份原权重计算SHA，与上表逐项匹配。缺文件时停止依赖权重的步骤，不运行cache/train/evaluate/pilot/confirm。
4. 独立只读CPU重放旧结果：先按上表验证文件SHA，然后用 `torch.load(..., weights_only=True, map_location='cpu')` 将原state_dict加载到原 `PathHead`；用原norm_stats、原20×10路径与原s_g1逐块计算MEAN/PATH分数。按date/code对齐原六份 `scores_*.npz`，要求 `max|Δ|≤1e-10`；只输出检查报告，绝不覆写旧分数。
5. 这一步是显式的历史只读重放，不走已拒绝旧上下文的生产 `_score_segment` / `cmd_evaluate`，不伪造新上下文。若重放超差，报告实际差异和环境，不调宽容差凑过。
6. 用 `git add -f` 精确补交上表两份 `.pt`，并提交新的验收说明。`git diff e4de646 -- artifacts/experiments/path_information_20260911` 除新增两份原权重与单独命名的新验收文件外应无改动；尤其不修改best/history/norm/cache/score/verdict。按仓库Co-Authored-By规范commit并push原分支，回传提交和测试/重放结果。

```bash
CUDA_VISIBLE_DEVICES='' PYTHONDONTWRITEBYTECODE=1 HF_HUB_OFFLINE=1 python - <<'PY'
import pandas
import pytest
raise SystemExit(pytest.main([
    'tests/test_path_information.py', 'tests/test_path_closeout.py',
    'tests/test_peer_residual.py', 'tests/test_peer_diagnostic.py',
    'tests/test_peer_production_gates.py', 'tests/test_qlib_mean_comparison.py',
    'tests/test_sae_residual.py', '-q', '-p', 'no:cacheprovider',
]))
PY
```

## 5. 完成与限制

Mac代码验收114项自足测试；含既有numpy数组转换弃用警告，不影响通过。原4项真实HF回归本轮Mac未运行，不宣称118项已在Mac通过。原266文件提交中的研究数据保持原字节，未进行真实训练、G1推理、数据库访问或GPU运算。

**整轮交付仍待原权重补交和CPU重放；不能仅凭代码测试通过宣布档案完整。** 研究配置继续封盘，不补种子、不扩展门控/TimesFM、不跑策略回测。
