# N=50 采样放大（论文敏感性曲线延长线）· 预注册实验计划

> 日期：2026-08-19
> 新分支：`feature/n50-amplify`，从 `feature/g7-shortwindow` 切出（首个提交 = 本计划）
> 立项依据：论文敏感性分析——IC/RankIC 随蒙特卡洛采样路径数 N 单调改善，**测到
> N=20 为止**；全项目一直用 N=20，曲线延长线从未检验。这是货架上唯一带论文背书的
> 未测旋钮。诚实先验：√N 降噪意味着 N=20→50 的估计噪声只再缩 ~37%，且论文曲线在
> N→20 已趋平——**预期效应小，中性结果（N2）是最可能的结局**；花一夜把它钉死，
> 换取"N=20 已充分"或"还有免费提升"的确定性。
> 执行解释器：`/home/user/miniconda3/envs/quant/bin/python`（DDB + RTX 5090）。
>
> **执行提示（agentic workers）**：按步骤顺序执行，checkbox 跟踪；每阶段贴实际输出。

## 0. 已确认事实（直接采信）

| 项 | 值 |
|---|---|
| 在位者 | G1 三种子 N=20 backtest 窗（2026-01-01~2026-07-24）mean AER(等权)：s100 +14.33 / s101 +29.06 / s102 +18.67 → 中位 s102 **+18.67% / +14.64%**（等权/指数） |
| 变量 | 仅 `sample_count`：20 → 50；其余推理口径逐字 canonical（L=90/H=10/T=1.0/top_p=0.9/seed=42）；G1 三种子权重只读 |
| 预算约束 | N=50 推理成本 ≈ 2.5× → 3 种子 × backtest 窗 ≈ ~11h（一长夜）。**2025H2 不跑**（预算裁定，跑前声明）——因此本轮若通过 N1，注册前置条件改为下一自然月登记数据自证（见 §2 注册行） |
| 登记表耦合约束 | C1/C2/C3 规则引用的是 **N=20 登记列**——无论结果如何，**既有登记列与组合规则定义不变**；N=50 若胜出只能作为**新增臂**（`G1N50_sXXX_*` 新列）另行登记，不替换任何在册定义 |
| 窗口纪律 | backtest 窗半污染（上限"待前向确认"）；forward 零判读 |

## 1. 臂（冻结）

| 臂 | N | 种子 | 窗 | 聚合 |
|---|---|---|---|---|
| N50 | 50 | 100/101/102 | backtest（2026-01-01~2026-07-24） | 四变体一次算全，判据只在 mean |

## 2. 判据（跑前冻结；三个出口都关闭 N 议题——不试 N=100，不做 N 网格）

| # | 判据 | 冻结定义 |
|---|---|---|
| N1 增量 | 中位种子 AER(等权) ≥ **+20.67%**（在位者中位 +2pp）且 AER(指数) ≥ +16.64% → "N 放大有增量，待前向确认"；**注册行**：以新增臂 `G1N50` 进 G3 登记表（新列，不改既有列/规则），登记推理成本 2.5× 由 cron 承受 |
| N2 中性 | 中位 ∈ (+16.67%, +20.67%) → "**N=20 已充分，边际收益耗尽**"，记录，不注册，不改任何东西（先验最可能结局） |
| N3 反常 | 中位 ≤ +16.67% → "更多采样反而更差 = 统计涨落警示"，如实记录并在结果文档讨论其对 N=20 在位数字稳定性的含义，不注册 |
| 措辞约束 | 中位对中位并列两族三种子分布；逐种子配对差（N50−N20 同种子）必列——这是同权重同窗同 RNG 家族的最干净配对对照 |

## 3. 步骤

- [ ] 3.1 建分支提交本计划；`n50_amplify/`（复用 g7 的配置覆盖模式，仅改 `sample_count`；
  **不改**任何既有目录）+ `test_n50_config`（断言除 sample_count=50 外与 canonical
  逐字段一致，含引擎参数冻结）先 FAIL 后 PASS；
- [ ] 3.2 3 种子 × backtest 窗四变体推理落盘（断点续跑，~11h）+ DuckDB 追加（对拍贴输出）；
- [ ] 3.3 引擎全表（N50×3 种子 + 在位者 N20 对拍行 + 逐种子配对差列）+ 族配色图；
- [ ] 3.4 N1~N3 一次封盘判读；结果文档 `docs/N50实验结果_<日期>.md`；提交推送。

## 4. 纪律

- 变量/种子/判据冻结；任何出口都不做 N 网格（50 之外不加点）；
- 评估数字在 3 种子信号全部落盘前不看；判读一次封盘；
- 既有登记列与 C1/C2/C3 规则定义在任何结果下都不变更；
- forward 零判读、G3 只写不读、G1 权重只读、引擎参数零改动——全部沿用；
- 每阶段贴实际输出，测试先失败后通过；提交规范同 CLAUDE.md，
  `Co-Authored-By: Hugo <shen.lan123@gmail.com>`。
