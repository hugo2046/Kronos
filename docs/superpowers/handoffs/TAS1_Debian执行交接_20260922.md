# TAS1：Debian 端 zcode 执行交接

> 交接日期：2026-09-22。接手对象：Debian 端 zcode。
> 任务：实现并依预注册门禁检验 TAS1，目标是击败项目 baseline。
> 用户要求：先创建分支，把计划和 handoff 一起提交到远端，zcode 拉取后据此执行。本文件随分支保存，覆盖 handoff 技能默认的系统临时目录规则。

## 1. 先拉取这个分支

- 远端：现有 Kronos 仓库的 `origin`，不是上游原作者的 `upstream`。
- 执行分支：`codex/tas-state`。
- 分支起点：`feature/h1-readout` 的 `fd2a2663dee90a858868146f60100011d773ecfe`。
- 本次交接只新增计划与本文件；模型代码尚未实现。以后产生的实现提交继续推送同一分支。

在 Debian **已有 Kronos 克隆的根目录**执行以下命令。先查看状态；若有未提交工作或同名分支存在分叉，保留现场并处理冲突，不能 reset、clean、强推或自动覆盖。

```bash
git rev-parse --show-toplevel
git status --short
git remote -v
git fetch origin

# 本地尚无该分支时：
git switch --track -c codex/tas-state origin/codex/tas-state

# 若本地已经有该分支，上面一行替换为：
# git switch codex/tas-state

git pull --ff-only origin codex/tas-state
git branch --show-current
git rev-parse HEAD
git rev-parse origin/codex/tas-state
git merge-base --is-ancestor fd2a2663dee90a858868146f60100011d773ecfe HEAD

cat docs/superpowers/handoffs/TAS1_Debian执行交接_20260922.md
cat docs/superpowers/plans/状态前置重读模型实验计划_20260922.md
```

验收：当前分支为 `codex/tas-state`；首次接手时 HEAD 与远端相同，且继承上述研究基线提交；计划和 handoff 均存在。若本地已有自己的未推送提交，先说明差异，不为了“对齐”丢弃提交。快进拉取失败时先解决分叉，不在错误分支开工。

如果当前工作目录因其他任务不能切换，可在本机现有 Git 工作树管理方式下为该远端分支创建隔离工作树；仍使用相同计划与分支，不另建一份实验协议。

## 2. 接手后读取什么

**单一实验依据**：[TAS1 研究与实施计划](../plans/状态前置重读模型实验计划_20260922.md)。完整模型、对照臂、数据切分、训练配方、判据、文件清单、测试、预算与停止条件都在那里，本交接不再复制。

按以下顺序恢复上下文：

1. 读取计划全文，特别是 §1 的基准、§3.4 的梯度/因果掩码、§6 的判据、§8 的阶段门禁和 §9 的预算。
2. 核对 Debian 仓库里实际适用的 AGENTS.md/CLAUDE.md；沿用中文沟通、Python 3.10+、pathlib、loguru、类型标注、公共接口 Sphinx docstring。新增测试放 `tests/test_*.py`。
3. 需要核对基线身份时，读 `docs/基准与样本外测试计划_20260810.md`；要理解最新历史证据时，读 `docs/多窗IC剖面结果_20260906.md` 与 `docs/H1实验结果_20260905.md`。旧收益口径的问题见 `docs/引擎v2重放对照_20260905.md`。
4. 需要定位代码时，若 `.codegraph/` 已存在，先用 CodeGraph；其 source 返回视为已读。若没有索引，先用 `rg`，初始化索引由用户决定，不因此阻塞实验实现。

后续计划统一放 `docs/superpowers/plans/`，设计文档放 `docs/superpowers/specs/`，交接放 `docs/superpowers/handoffs/`。沿用中文功能名与日期，避免新计划散落在 docs 根目录。

## 3. 当前进度与授权

已完成：论文阅读、仓库基准与最新结果核对、相关模型接口调查、研究方案落盘、文档结构与引用检查。当前任务负责交接文档的提交和推送，是否取得本次交接以实际远端 Git 记录为准。

未完成：`tas_state/` 包、`tests/test_tas_*.py`、可执行 config/manifest、Debian 环境核验、模型训练、历史评估与新前向登记。文档中的 CLI 是待实现接口，不能直接当成已有命令运行，也不能把未勾选的计划写成完成。

用户已明确要求 zcode 按 handoff 执行。接手后直接从 P0/P1 实施推进，不停在“是否开始实现”的重复确认；P2/P3 依据门禁自动决定是否进入。预算超限、需要改变研究主假设/判据、实际资源不可获得或存在不可安全解决的冲突时，再带具体证据提出问题。

本任务目标是 Kronos 金融预测模型，不是 Agent 状态卡或会话记忆优化。前面讨论曾误入该方向，仓库核对后已纠正，以随分支计划为准。

## 4. 实际执行顺序

### 第一步：P0 环境与协议，先做能做的检查

1. 核实 Debian 的解释器、PyTorch/CUDA/GPU、存量数据、权重与信号。历史文档使用过 `quant` conda 环境；实际路径和依赖以当前机器为准，不照搬 macOS 绝对路径。
2. 检查现有任务及16:30登记安排，协调 GPU 空档；保留已有任务运行，不杀进程抢资源。
3. 新建 `tas_state/config.py` 和实际加载的 `tas_state/config.json`，把计划参数固化；记录当前代码与数据/权重哈希。计划CLI所引用的config.json必须随实现提交，不能遗漏。
4. 建立manifest与覆盖/封存检查，先完成无需新模型的基线核验。

### 第二步：P1 实现，再完成需要模型的 P0 测量

P0 里的 wrapper 旁路对拍、TAS耗时与显存测量依赖 P1 代码。按依赖交错完成：实现数据与模型、跑玩具测试和 baseline 旁路，然后回补 P0 的真实资源试跑及 B1 预算。不要为了遵守编号顺序调用不存在的模型，也不要在 P0 尚未完整通过时开始 P2 正式训练。

使用计划 §7 的新增文件边界，按 §8 先写关键失败测试，再实现最小模型。复用现有模型模块、数据入口和引擎，不改已封存实验。重点验证第一遍无未来输入、第二遍梯度可传回状态编码器、细 token 注意力掩码正确、采样 token 对拍和IC列序不变。

阶段验收：config/manifest已冻结，所有 P0/P1 门禁有真实输出，资源预算可行。

### 第三步：P2 seed42，先落全信号再一次开封

按计划训练两个模型族、通过验证段选点、生成全部预设对照和两个历史窗口信号。先核对覆盖与哈希，再执行唯一的历史判读。失败时写结果并按协议关闭，不能换聚合、窗口或seed找通过。

### 第四步：条件 P3 和 P4

- P2 满足计划 §6.2 阶段A才追加43/44；完成三种子与集合预算对照后，按阶段B判读。
- P3满足全部条件才建立全新前向登记；登记首日严格晚于冻结，不向旧forward回填新模型。
- 前向信号登记是长期阶段，不要求本次运行空等126个交易日。完成合法登记与测试后交付状态，依正式调度执行；最终结算必须等标签成熟。
- 原forward协议保持封存，新实验不得提前读取它来挑方案。只有计划白名单中的旧决策日结算缓冲可用于历史评估。

## 5. 推进时怎样留证据

每个阶段结束时更新计划checkbox，并在结果文档记录实际命令、退出码、测试结果、配置哈希、当前提交、用时和下一步。训练权重、大型缓存、信号parquet等遵守已有gitignore；先检查拟提交清单，再添加具体文件，不用 `git add .` 混入机器索引和无关工作。

在同一分支按逻辑阶段提交，使用项目要求的中文提交类型与正文格式；Co-Authored-By沿用本次交接提交的项目约定。提交前查看暂存diff，运行该阶段适用的测试，随后正常 `git push origin codex/tas-state`；不强推，不擅自合并主线。

遇到阻塞时交付：阻塞项、实测错误、已经完成的检查、可独立继续的任务、需要用户补充的最小信息。GPU不可用时可以继续完成CPU玩具测试；未来数据未成熟时不能宣称实验已胜出。

## 6. 建议调用的 skills（suggested skills）

接手代理如有 Skill 工具，按名称调用；其他环境按其技能加载方式读取对应 SKILL.md。Debian 安装路径自行发现，不使用本机 macOS 路径。

| 技能 | 触发时机 |
|---|---|
| `using-superpowers` | 接手开始时，建立技能与任务流程 |
| `executing-plans` | 主执行技能，跟踪随分支计划的P0～P4 |
| `writing-plans` | 某实施阶段需要细化代码任务时，在现有研究协议下拆步骤；不重新选择研究方向 |
| `test-driven-development` | 实现数据、模型和评估行为前，建立有效失败测试 |
| `factor-evaluation` | 实现RankIC、配对差、HAC和多重检验；组合层服从项目long-only口径 |
| `systematic-debugging` | 遇到掩码、对齐、梯度或对拍失败时，先查根因 |
| `verification-before-completion` | 每次宣称阶段通过、提交与推送之前核验真实输出 |
| `handoff` | 换会话或暂停时更新本交接，保持随分支可恢复 |

技能默认流程如要求重新确认已经授权的实施，优先使用本交接中的用户授权；若技能提供多空示例，则服从本项目long-only约束。技能不可用时记录具体缺失，不声称已调用。

## 7. 接手后的第一条进展报告

报告实际拉取的分支与HEAD、当前工作区状态、计划阅读完成情况、Debian资源核验结果，然后立即推进P0/P1。任何“已实现”“测试通过”“已训练”“胜出”都必须对应可检查产物与实际输出。
