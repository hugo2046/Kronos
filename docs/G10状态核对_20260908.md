# G10 状态核对（20260908）

> 性质：状态调查，**非 G10 实验计划、非启动授权**（纠偏计划 §7 明令禁止自动
> 启动）。核对范围：Debian 本地/远端分支、全部已取回提交、`docs/`、模型输出
> 目录、运行进程；未扫描读取 registry forward 内容。

## 0. 结论

**只发现计划，未证实执行。** A0~A4 五臂均无任何实现/训练/评估证据；无进行中
进程。G10 如需启动，需先独立预注册新评估口径（见 §3）。

## 1. 证据定位（按臂）

| 臂 | 路线图定义（`docs/研究路线图v2_20260903.md` §第 2 阶段，L47-51） | 状态 | 核对出处 |
|---|---|---|---|
| A0 全参数（对照+D3 亚 epoch 曲线） | 102M | **未找到执行证据** | 模型目录仅 f1/g1/g2(5 种子)/g8(3 种子)/dtok（`finetune_suite/outputs/models/`，无 g10/a0/d3 亚步 checkpoint）；`git log --all --grep` 无 G10 训练提交 |
| A1 DualHead（粗头+细头） | 1.7M | **未找到执行证据** | `DualHead` 仅存在于上游模型代码 `model/kronos.py`/`module.py`（论文架构自带，非微调产物）；无 A1 模型目录/提交 |
| A2 DependencyAwareLayer | 2.8M | **未找到执行证据** | 同上：`DependencyAwareLayer` 为上游架构组件（`backbone.py` 亦引用其定义做冻结底座），无 G10 微调产物 |
| A3 HierarchicalEmbedding | 3.1M | **未找到执行证据** | 仓库内 `HierarchicalEmbedding` 命中均为上游代码（含历史提交 `a7e294c` "define missing split_token"，2026-09-03 前的 Kronos 代码修复，非 G10） |
| A4 12 层 q/k/v/o LoRA(r=8) | 0.64M | **未找到执行证据** | `LoRA` 命中为 dhead_distill 实验（`7677620`/`e5f10c3`，DHead D2 臂，另一实验线）与上游代码，非 G10 A4 |

## 2. 全局检索记录

- 提交：`git log --all --oneline --grep="G10" -i` → 仅 `55e76b7`（路线图）与
  `9d5128f`（本轮核对计划）；`--grep="受限适配|DualHead|LoRA|Hierarchical|
  DependencyAware"` → 仅 dhead_distill 族与上游代码修复；
- 分支/工作树：无 g10 相关分支；工作树三处（主树、dhead-distill、本轮纠偏），
  无 G10 代码树；
- 目录：无 `g10*` 目录；`finetune_suite/outputs/models/` 清单见上；
- 进程：`pgrep` 无 g10/训练类进程在跑；
- 路线图自身（L71）把 G10 记为"第 2 阶段（主实验）"的**计划**，其预算/夜跑
  安排均为前瞻描述。

## 3. 判读与前置条件（不启动）

1. 路线图 Q0 复现锚为"G1 s100 等权 AER +14.33%（差 ≤1pp）"、Q3 用旧噪声
   阈值——该锚点属**旧引擎口径**（engine v2 六修正重放已判 64/259 臂翻转、
   G1 +14.33%→+1.90% idx 转负，见 `docs/引擎v2重放对照_20260905.md`）。本轮
   MH1 修正回测**不得**被用来悄悄替换旧判据后宣布 G10 通过或失败；
2. 若启动 G10：需独立预注册新评估口径（判据锚、噪声底、窗口、引擎版本全部
   重冻结），并单列预算（路线图估五臂训练+10 组推理 ≈12h）与授权；
3. 决策建议（仅建议）：**已有结果先审结果（无）；在跑则尊重现有任务（无）；
   只有计划 → 待独立审批/预注册后方可启动**。证据链完整，无最小缺失项。
