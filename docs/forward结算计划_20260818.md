# Forward 结算计划（2026-11 执行）· 判据冻结文档

> 冻结日期：2026-08-18（forward 窗可结算交易日数 = 0，无人见过任何 forward 绩效数字）
> 执行时点：forward 窗（2026-07-25 起）累积 **≥60 个可结算交易日**（H=10 口径），
> 预计 2026-11 上旬；执行前以交易日历断言。
> 分支：`feature/g4-features` 谱系；执行时可切新分支 `feature/forward-settlement`。
>
> **本文档提交后至结算执行前不得修改**。唯一例外：登记数据完整性事件（缺日、哈希
> 断链），须公开披露并只做披露性补充，不得触碰判据。

## 0. 前言：Q2 预承诺（用户 2026-08-18 书面确认）

> **若首读 G1 判据（§3.1）为负——接受关闭。** Kronos-alpha 主张就此收官，写终版
> 总账文档，不做参数搜索、不换窗口、不以任何"再等等/再试试"延长项目。

对称地，正向结果的措辞上限同样预先写死：首读通过 ≠ 终审——60 个交易日的读数
t 值约 0.4，只是方向检查点；通过的结论是"**前向首读通过，进入滚动确认**"
（下一读数在再累积 60 个交易日后），不是"有效"。

## 1. 数据源与证据分级

| 级别 | 定义 | 适用臂 |
|---|---|---|
| **登记级**（最高） | 信号在结果发生前逐日落盘、SHA256 入 MANIFEST、git 盖章（G3，2026-08-14 起） | G1 三种子四变体、M、MA200 门控、C1/C2（登记列的确定性函数） |
| 复算级 | 权重/协议在结算前已冻结，结算时确定性重放生成 forward 信号（事后计算，先后无密码学证明） | F0 zero-shot（官方权重+canonical 协议+seed42）、F1（第 4 轮权重）、B3（冻结 checkpoint）、R1 原版（M/F0 切换） |

判读时两级分开成节，登记级为主；复算级结论一律附"复算级"标签。

## 2. 结算对象（冻结，禁止增删）

组合口径全体一致：csi300 池、k=50/n=5/min_hold=5、单边 15bp、双基准
（000300.SH + 同池等权）、mean 变体为判据口径（四变体记录族全表呈现，不判读）。

| # | 臂 | 级别 | 携带的历史结论 |
|---|---|---|---|
| 1 | **G1 三种子**（s100/101/102） | 登记级 | 多种子存活（半污染窗），本轮为其首读 |
| 2 | C1 三种子集成 | 登记级 | 规则冻结于 `docs/组合规则预注册_20260816.md` |
| 3 | C2 MA200 切换 | 登记级 | 同上；归因已判"G1 含非动量增量"，按互补臂解读 |
| 4 | M 动量 | 登记级 | 免费裁判 |
| 5 | F1（第 4 轮权重） | 复算级 | "改善但未存活"，观察其 forward 方向 |
| 6 | F0 zero-shot + R1 原版切换 | 复算级 | F0 为链条零点参照；R1 携带第 2 轮"状态假说获支持" |
| 7 | B3（冻结 checkpoint） | 复算级 | 已降级"疑似种子运气"，复核用 |

## 3. 预注册判据（本文档冻结，结算时只做代入）

### 3.1 G1 主判据（绑定 Q2）

**G1 三种子 mean 变体的中位种子**：forward 结算窗 AER(等权) > 0 **且** AER(指数) > 0
→ "**前向首读通过，进入滚动确认**"；任一 ≤ 0 → **触发 Q2 预承诺：项目关闭**，
产出终版总账（§5）。

### 3.2 其余判据

| # | 判据 | 冻结定义 |
|---|---|---|
| C1-存活/增值 | 按 `组合规则预注册_20260816.md` §3 原文代入（AER 双正；> G1_s100 同窗） |
| C2-存活/增值 | 同上（AER 双正；> max(纯 M, 纯 C1) 同窗） |
| R1 原版 | AER(等权) 双基准 > 0 → 第 2 轮"状态假说"前向存活（复算级） |
| F1 方向 | F1_mean > F0_mean 同窗 → "本土化改善方向前向复现"（复算级；不设存活判据） |
| B3 复核 | AER(等权) ≤ 0 → "种子运气"定谳；> 0 → 记录为未解释残差，不复活路线 |
| 全表纪律 | 七臂全数呈现，无挑选；G4/G5 已关闭臂不进结算 |

### 3.3 完整性与敏感性（判读前置）

- MANIFEST 哈希链逐日校验 + 缺日/late 日清点，先于任何绩效计算并入文档；
- C2 对 late 日的剔除敏感性（规则文档冻结条款）；
- 登记信号 vs DDB 复算抽样对拍（≥5 日 × 3 臂，`max|Δ|` 贴实数）。

## 4. 执行步骤（结算时逐项 checkbox）

- [ ] 4.1 交易日历断言 ≥60 可结算日；完整性审计（§3.3）；
- [ ] 4.2 复算级臂 forward 信号生成（F0/F1/B3 推理 + R1 组装，断点续跑）；
- [ ] 4.3 七臂过引擎全表 + 族配色净值图（登记级/复算级分节）；
- [ ] 4.4 判据 §3 一次性代入判读，封盘；结果文档 `docs/forward结算结果_<日期>.md`；
- [ ] 4.5 按 §3.1 分支执行 §5 或 §6；提交推送。

## 5. 关闭分支（G1 首读为负）

终版总账 `docs/Kronos项目终版总账_<日期>.md`：全部 8 轮实验谱系、关闭路线清单、
方法论产出（预注册纪律的实战记录）、负结果的完整可复现指引；G3 登记 cron 停止；
仓库归档。**不做任何挽救实验。**

## 6. 滚动分支（G1 首读通过）

- 下一读数 = 再累积 60 个可结算交易日（约 2027-02），判据沿用 §3 不重设；
- C1/C2 通过增值判据者升格为在位组合候选，G3 登记续跑；
- 任何新研究（G4b 个股长程状态列等）须另立预注册，且以通过首读的臂为在位者。

## 7. 纪律

- 本文档即判据的唯一来源；结算文档禁止出现本文档之外的通过/失败标准；
- 结算前继续零判读（登记只写不读）；执行时开封一次；
- 措辞约束沿用：登记级/复算级分级标注；"通过"上限为"待滚动确认"；
- 提交规范同 CLAUDE.md，`Co-Authored-By: Hugo <shen.lan123@gmail.com>`。

---

## 附录 A · 登记数据完整性事件披露（2026-08-20）

> 按 §0 例外条款（"登记数据完整性事件须公开披露并只做披露性补充，不得触碰判据"）记录。
> **本附录不修改任何判据。**

**事件**：G3 前瞻登记 cron 于 2026-08-20 经用户决定停止运行。

**后果（结算时按 §1 证据分级机械适用）**：

| 时段 | 证据级别 | 说明 |
|---|---|---|
| 2026-08-14 ~ 2026-08-18（manifest 末条） | **登记级** | 信号在结果发生前落盘、SHA256 + git 盖章，ex-ante 证明完整 |
| 2026-08-18 之后 ~ 结算日 | **复算级** | 权重与协议已冻结、可确定性重放，但**无先后密码学证明** |

**结算执行影响**：`settlement/` 包的复算级臂生成器（F0/F1/B3/R1 已实现）需扩展覆盖
G1 三种子 + M + MA200 门控在断点之后的日期；C1/C2/C3 作为登记列的确定性函数随之
同级降档。判读文档须在登记级/复算级分节中如实标注两段时间的分界。

**不可恢复性说明**：断点之后的信号可以事后重放得到相同数值，但"在结果发生之前
即已承诺"这一属性无法追溯补齐——该时段的证据强度永久为复算级。
