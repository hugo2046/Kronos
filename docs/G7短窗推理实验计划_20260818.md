# G7：G1 底座短窗推理复检（L=8 经验先验，两配置）· 预注册实验计划

> 日期：2026-08-18
> 新分支：`feature/g7-shortwindow`，从 `feature/g4-features` 切出（首个提交 = 本计划）
> 立项依据：用户实盘经验先验（短回看窗）。第 2 轮曾在**零样本权重**上测 L=8/H=5
> （C1 臂，-3.76%/-11.35% 两窗全负）并关闭"推理期参数"路线——该判决范围限于
> 零样本；G1（本土化微调、多种子存活）上短窗从未测过，假说从"短窗能否救塌方"
> 变为"短窗能否改善活信号"，按 G5 先例以新预注册重开（G5 重开换头亦死，先验照此打折）。
> 执行解释器：`/home/user/miniconda3/envs/quant/bin/python`（DDB + RTX 5090）。
>
> **执行提示（agentic workers）**：按阶段顺序执行，checkbox 跟踪；每阶段贴实际输出。

## 0. 已确认事实（直接采信）

| 项 | 值 |
|---|---|
| 在位者 | G1 原版（L=90/H=10）三种子 backtest 窗 mean AER(等权)：s100 +14.33 / s101 +29.06 / s102 +18.67 → **中位 = s102：+18.67%（等权）/ +14.64%（指数）** |
| G1 工件 | 三种子微调 tokenizer+predictor 在生产机，只读；L/H 是**纯推理期参数**（`pred_len` 入参 + 上下文截断），换配置零训练 |
| 微调几何 | 语料窗口 = 90+10+1 固定长；AR 因果训练使 L=8 前缀**在分布内**，但推理端窗口 z-score 仅 8 点、统计噪声大（第 2 轮 C1 预警过，如实沿用） |
| 引擎错配披露 | 引擎 canonical 冻结（k=50/n=5/**min_hold=5**/15bp）：H=3 信号与最短持有期错配（预测 3 日、被迫持 5 日），引擎不动，错配入判读措辞；H=5 无此问题 |
| 用户先验的两个版本 | 2026-08-11 说"8 天看 5 天"、2026-08-18 说"8 天看 3 天"——**两配置均冻结进候选，禁止事后挑** |
| 窗口 | backtest 2026-01-01~2026-07-24 + 2025H2（边界与在位者一致保可比；H 短导致的多余可结算日不使用）；半污染声明沿用 |
| 推理口径 | 除 L/H 外逐字 canonical：N=20/T=1.0/top_p=0.9/seed=42，mean 判据 + 四变体记录族 |

## 1. 臂（冻结）

| 臂 | L | H | 种子 | 成本 |
|---|---|---|---|---|
| W83 | 8 | 3 | 100/101/102 | 短窗推理远快于 90/10 |
| W85 | 8 | 5 | 100/101/102 | 同上 |

2 配置 × 3 种子 × 2 窗 × 四变体，一次推理四聚合算全；预算合计 ~一夜。

## 2. 判据（跑前冻结）

| # | 判据 | 冻结定义 |
|---|---|---|
| K1 增量 | 某配置中位种子 backtest AER(等权) ≥ **+20.67%**（在位者中位 +18.67%+2pp）且 AER(指数) ≥ +16.64% → "短窗有增量，待前向确认"，注册进 G3 |
| K2 中性带 | 中位 ∈ (+16.67%, +20.67%) → "短窗中性"，记录不注册 |
| K3 否定 | 两配置中位均 ≤ +16.67%（在位者−2pp）→ "**短窗在本土化底座上亦无增量，L/H 议题两代底座终审关闭**"，不做窗口搜索 |
| K4 跨窗 | K1 通过者 2025H2 AER(等权) > 0 方可注册 |
| 措辞约束 | 中位对中位并列呈现两族三种子分布；H=3 结果的判读必须附 min_hold 错配注记 |

## 3. 步骤

- [ ] 3.1 建分支提交本计划；`g7_shortwindow/`（复用 improve/finetune 推理链路，仅覆盖
  L/H 参数；**不改**任何既有目录）+ `test_g7_config`（断言除 L/H 外与 canonical 逐字
  一致）先 FAIL 后 PASS；
- [ ] 3.2 12 组推理落盘（断点续跑）+ DuckDB 追加（对拍贴输出）；
- [ ] 3.3 引擎全表（2 配置 ×3 种子 ×2 窗 + 在位者对拍行）+ 族配色图；
- [ ] 3.4 K1~K4 一次封盘判读；结果文档 `docs/G7实验结果_<日期>.md`；提交推送。

## 4. 纪律

- 配置/种子/判据冻结；K3 触发不做任何 L/H 搜索（8/3 与 8/5 之外不加点）；
- 评估数字在 12 组信号全部落盘前不看；判读一次封盘；
- forward 零判读、G3 只写不读、G1 权重只读、引擎参数零改动——全部沿用；
- 每阶段贴实际输出；提交规范同 CLAUDE.md，`Co-Authored-By: Hugo <shen.lan123@gmail.com>`。
